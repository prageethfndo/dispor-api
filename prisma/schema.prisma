generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["mongoDb"]
}

datasource db {
  provider = "mongodb"
  url      = env("MONGO_URI")
}

model FDO {
  id   String @id @default(auto()) @map("_id") @db.ObjectId
  name String
  age  Int
}
